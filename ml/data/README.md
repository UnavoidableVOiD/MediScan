Datasets are downloaded by `make data` and verified against `manifest.json` (sha256). Never commit data here.
Never place any real patient report here — golden-set PDFs go to `ml/golden_set/pdfs/` (also gitignored).
