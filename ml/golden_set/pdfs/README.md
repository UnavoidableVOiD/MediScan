Real lab reports for the OCR benchmark. Gitignored. Store the canonical copy in the private bucket
`mediscan-golden-set`; keep consent/redaction record per file in `../truth/<id>.json` under `provenance`.
